# Статус · 31.07, 15:20 MSK

## Сделано

- **Чтение сайта** по адресу: обход до 15 страниц (глубина 2, приоритет
  услуг/цен/контактов, sitemap, варианты www, обход бот-блока рендером),
  извлечение фактов сильной моделью строго по словарю, каждый факт с
  `source_url` и `origin=crawled`.
- **Поток стадий** `GET /api/site-read` (SSE): «Открываю сайт» → «Читаю
  разделы» → «Собираю факты» → «Готово: собрано N%», где N — доля закрытых
  ключей словаря. Строки лога и счётчик фактов идут по реальным событиям.
- **Интервью в режиме GAPS**: после чтения задаются только пробелы (5–7
  вопросов), спрашивать уже известное невозможно по построению. Сайт не
  прочитан → честная реплика, флаг `SCRAPE_FAILED`, полный путь на 13 вопросов.
- **Переспроса «Продолжаем?» нет**: дан адрес — агент читает сразу.
- **Вёрстка выбора агента** приведена к прототипу ред. 8 по списку дизайнера
  (кавычки, капс mono-подписи, сетка, отступы 18×19, радиус, ховер, лого-бар).
- **Зона дизайна** `src/components/ui/**` заведена: вёрстка финальная, данные
  через props; правило зафиксировано в `CLAUDE.md` репозитория.
- **Сквозной прогон** доведён до чата: приветствие, ответ по фактам, контакт,
  лид, печать трейса. Артефакты — в этом репозитории.

Деплой: коммит `ab41c2f`, Production, состояние читается из GitHub Actions
(workflow `deploy-status`, событие от vercel[bot]).

## Мешает

**Найден баг по живому прогону владельца** (`/t/resesarchlabs-8c53`):
карточка осталась почти пустой — сайт по этому адресу не открылся, — но
агент всё равно ведёт диалог и повторяет одну и ту же реплику
(«Мы — resesarchlabs.ru, и у нас есть сайт, где всё подробно описано»).
Два дефекта:

1. При пустой карточке агент обязан честно говорить, что данных нет
   (P5.1), а не изображать осведомлённость. Фраза «у нас есть сайт, где всё
   подробно описано» — не факт карточки.
2. Повтор одинакового ответа подряд ничем не ловится.

Чиню обоими концами: честный отказ при пустой карточке и защита от повтора.

## Не хватает

- Владелец создаёт публичный репозиторий `gb-bridge` (одна кнопка) — прокси
  сессии не даёт создавать репозитории:
  `sessions are bound to their configured repositories`. После создания —
  пуш моментальный.
- Трейс из боевой базы: приложу после живого прохода — выгрузка идёт мостом.
- Текста P5.7 нет ни в одном присланном документе (в пакете промптов блок
  заканчивается на P5.6).
