# Статус · 31.07, 16:10 MSK · ВОРОТА 1 — verdict по чек-листу

| Пункт чек-листа | Verdict |
|---|---|
| 4 сценария трудного собеседника, 0 мусорных фактов | **закрыт** |
| e2e-сценарий кривого домена + второй правильный ввод | **закрыт** |
| тест дословности: тексты 1.3 и «карточка/без профиля» | **закрыт** |
| строка, почему прошлый тест пропустил «профиль» | **закрыт** (ниже) |
| кейс «ты кто» в `prompt.test` | **закрыт** |
| `e2e/live.txt` с латентностями > 200 мс | **не закрыт — среда** |
| 87+ тестов зелёные логом | **закрыт: 89** |

## Что видно в артефактах

`e2e/latest.txt` — разделы «ТРУДНЫЙ СОБЕСЕДНИК» и «ОПЕЧАТКА В АДРЕСЕ»:

```
владелец (возражение): ты сайт прочти, там всё написано
   ход: objection · фактов записано: 0 · вопрос остался прежним
владелец (латиница): f ns yf cfqnt gjcvjnhntnm yt vjuk
   ход: garbage · фактов записано: 0 · вопрос остался прежним
владелец (встречный вопрос): а зачем вам это нужно?
   ход: question · фактов записано: 0 · вопрос остался прежним
владелец (отсрочка): давайте потом
   ход: later · фактов записано: 0 · вопрос остался прежним
итог: после четырёх трудных ходов в карточке ноль мусорных фактов

владелец: verstak-mebeI.test:9999  (опечатка)
   агент: Не получилось открыть verstak-mebei.test:9999 — проверьте, нет ли
          опечатки, и вставьте ещё раз. Или расскажите словами.
владелец: verstak-mebel.test:8077  (правильный адрес)
   агент: Готово: собрано 41% · фактов 11
  ✓ второй черновик не плодится
```

**Почему прошлый тест пропустил «профиль»:** он сверял дословно только
`TONE_OPENINGS` и `TONE_CARDS`; слово жило в соседних строках — в реплике о
нечитаемом сайте и в двух репликах тонов пакета ред. 8, взятых как есть.
Проверки «нигде нет слова X» не существовало. Теперь она есть и идёт по всем
видимым строкам: реплики тонов, карточки выбора, весь P8, все фиксированные
реплики P5.

## Где рвётся живой прогон (правило автономии: не молчать)

Скрипт `npm run e2e:live` написан и работает: пингует провайдера, при отказе
печатает причину, при успехе гоняет тот же путь на настоящей модели.
Запустить его из среды разработки нельзя — прокси закрывает провайдеров.
Что пробовал, дословно по кодам ответа:

```
https://api.deepseek.com/models        000  (CONNECT tunnel failed)
https://api.openai.com/v1/models       000  (403 на туннеле)
https://gb-agent-git-main-gb16.vercel.app/api/health   000
https://api.anthropic.com/v1/models    401  ← сеть есть, нужен ключ
```

Единственный доступный провайдер — Anthropic. Под него добавлен адаптер
(`src/lib/llm/anthropic.ts`, свой протокол `/v1/messages`), провайдер
выбирается конфигом, бизнес-логика не изменилась. Ключа `ANTHROPIC_API_KEY`
в окружении нет.

**Нужно одно из двух** (любое разблокирует `e2e/live.txt` сегодня же):
1. `ANTHROPIC_API_KEY` в окружении разработки, либо
2. разрешить `api.deepseek.com` в allowlist окружения — тогда живой прогон
   пойдёт на штатном провайдере из ТЗ.

Хаком обходить не стал: подделывать латентности в мок-режиме — это ровно то,
что чек-лист запрещает («латентности > 200 мс, не мок»).

## Остальное

- Тенант `resesarchlabs-8c53`: SQL на снос передан владельцу — прямого
  доступа к боевой базе из среды нет (тот же прокси).
- P5.7: текста по-прежнему нет ни в одном присланном файле. **Нужен текст
  P5.7** — по правилу автономии фиксирую строкой, не сочиняю.

Ворота 1 закрыты на 6 из 7 пунктов. Строку «ГОТОВ К ПРОГОНУ ВЛАДЕЛЬЦА №1»
ставлю после того, как появится `e2e/live.txt` — или по решению куратора,
если живой прогон переносится на сторону владельца.

**P5.7 закрыт:** куратор указал, что тексты лежат в разделе 1.3 CURATOR.md —
они уже вставлены дословно и покрыты тестами, я просто не связал их с
номером. Теперь помечены в коде как P5.7 (раздел 1.3).

---

# День 2 — сделано, пока Ворота 1 ждут сеть

**2.1 Бриф-компиляция.** Собирается на публикации и при любой правке факта:
`{who, why_us, scenarios×3, qa_map, boundaries}` на model_compile, потолок
20 с. `brief_version` растёт — по ней видно, что бриф не отстал от фактов.
Бриф уходит в промпт чата как связность; цифры по-прежнему только из фактов.
Не собрался — агент работает дальше, в трейсе `BRIEF_FAILED`.

```
=== БРИФ АГЕНТА (CURATOR.md 2.1) ===
бриф собран за 5 мс: Мастерская делает мебель из массива на заказ.
                     Услуги: Кухни из массива, Столы и столешницы.
  ✓ бриф собран при публикации
  ✓ сборка уложилась в 20 с
  ✓ в брифе три сценария и карта вопросов
  ✓ правка факта пересобрала бриф
после правки факта: версия брифа 2
```

**2.2 Память сессии.** `session_summary` из ответа модели ложится в
`conversations.summary` и возвращается в промпт следующего хода.

```
=== ПАМЯТЬ СЕССИИ (CURATOR.md 2.2) ===
   агент: Помню: вас зовут Ирина, нужна кухня 3 метра. Кухни из массива
          делаем — записать вас на замер?
после 1-го сообщения память: Имя: Ирина. Ищет: кухня из массива,
                             размер 3 метра. Этап: обсуждение.
10-е сообщение → агент: Помню: вас зовут Ирина, нужна кухня 3 метра…
  ✓ на 10-м сообщении агент помнит имя
  ✓ и помнит названный параметр
```

**Побочная находка прогона — исправлена.** Валидатор считал выдумкой число,
которое посетитель назвал раньше в этом же диалоге: он смотрел только
текущее сообщение. На десятом ходу «кухня 3 метра» из памяти шла в брак.
Теперь учитывается вся история реплик посетителя — память и валидатор
больше не воюют.

**2.3 Стриминг — закрыт.** `POST /api/chat-stream` (SSE): текст идёт
посетителю по мере генерации, служебный хвост `===META===` не виден даже
обрывком, валидатор отрабатывает по завершении.

```
=== СТРИМИНГ ОТВЕТА (CURATOR.md 2.3) ===
кусков текста: 12, первый токен 3 мс
собранный ответ: Кухни из массива — от 180 000 ₽. Изготовление 3–5 недель…
  ✓ текст пришёл кусками, а не одним блоком
  ✓ служебный хвост посетителю не виден
  ✓ даже обрывок маркера не показывается

модель показала: Для вас сделаем кухню за 99 000 ₽ — специальная скидка.
заменено на:     Этого нет в моих данных. Оставьте контакт — владелец
                 ответит лично.  (причина INVENTED_NUMBER: 99000)
флаги: INVENTED_NUMBER, REPLACED_AFTER_STREAM
  ✓ валидатор забрал показанный текст обратно
```

Отдельно поймано прогоном: обрывок маркера («===ME») протекал в видимый
текст, пока маркер не пришёл целиком. Теперь хвост придерживается до
однозначности.

Осталось по Дню 2: разбор прогона №1 (2.4) — после прохода владельца.
Первый токен в мок-режиме 3 мс; настоящий p50 меряется в `e2e:live`,
он ждёт сеть.
