# Grand Bazar · gb-agent — мост

<!-- BRIDGE:STATE -->
Состояние на 06.08.2026 · ветка `main`, коммит `c5e82cb` · тестов зелёных: **970** · прод (/api/health): `d4f857d`
<!-- /BRIDGE:STATE -->

Блок выше обновляет `./bridge.sh` — **прогонять после каждого мержа, это
правило, а не пожелание**. Тот же прогон ПУБЛИКУЕТ мост в открытый
репозиторий `gb-bridge` (HANDOFF.md + свежие тесты, e2e, live, трейсы):
куратор читает только его — этот приватный репозиторий для него не
существует. Документ написан для человека, который берёт код в работу
первым днём: сначала что это, потом где что лежит, потом мины.

---

## 1. Что это за продукт

**Карточка** — публичная страница дела по постоянному адресу, устроенная
сразу для двух читателей: человек читает глазами, машина забирает
разметку. Внутри страницы живёт агент, который отвечает посетителям
**строго по записанным фактам** и доставляет заявки владельцу на почту.

Карточка собирается **разговором за пять минут, без сайта**: человек
разговаривает с агентом как с живым собеседником, слева на глазах
заполняется его карточка, в конце — ссылка на витрину и секретная ссылка
правки. Второй вход — **чтение сайта** — сохранён за кнопкой
«У меня есть сайт»; он в ~20 раз дороже и нужен не всем.

### Чего продукт не делает и делать не должен

- Не пишет ничего, чего владелец не говорил. **Пробел честнее выдумки** —
  правило проходит через весь код и заперто тестами и сторожами.
- Не выдумывает отзывы и оценки; `aggregateRating` не выводится никогда.
- Не обещает видимость в чужих ИИ, не показывает проценты понимания.
- Наружу — слово «карточка» (приказ владельца 06.08; прежнее «лавка» из
  пользовательских текстов снято целиком). «Паспорт» — внутреннее: схема,
  код, `/gb-debug`. Машинные имена (`/api/lavka/<slug>.json`,
  `lavkaSchema.ts`) не переименовываются — это контракт. `/api/lavka/…`
  живёт как ЛЕГАСИ навсегда, но любое НОВОЕ внешнее машинное имя рождается
  от `card` (сторожит `tests/machineNames.test.ts`).

---

## 2. Стек и адреса

| | |
|---|---|
| Фреймворк | Next.js 15 App Router, React 19, TypeScript, обычный CSS |
| База | Supabase (Postgres) через REST, сервис-ключ, без SDK |
| Тесты | vitest (число — в блоке состояния выше) + 4 сквозных пути на моках |
| Хостинг | Vercel, проект `gb16`; деплой едет за `main`, руками не трогается |
| Прод | `https://gb-agent-git-main-gb16.vercel.app` · `/api/health` показывает живой коммит |

Домена пока нет — это по-прежнему блокер постоянных адресов.
Дизайн-граница: `src/components/ui/**` и `passportMarkup.ts` — источник
истины из дизайна, разработка только подключает данные.

---

## 3. Маршруты

Страницы: `/` → `/start` (разговорный вход, двухпанельный с первой
секунды; на телефоне — чат во весь экран, карточка полоской сверху с
раскрытием по тапу) · `/start?site=1` (чтение сайта) · `/t/<slug>` (карточка + агент) ·
`/t/<slug>/edit?t=<токен>` (правка + блок «Заявки») · `/gb-debug?token=…`.

`POST /api/onboarding`, ветвится по `action`. Разговорный вход:
`talk` · `talkAnswer` · `talkFinish`. Путь через сайт: `intro` · `mirror`
· `scan` · `extract` · `nextStep` · `answer` · `retrySite`. Общие:
`publish` · `updateFact` · … · `deleteShop` (владелец сносит свою карточку
по секретной ссылке). Плюс `/api/chat*`, `/api/lead`,
`/api/lavka/<slug>.json`, `/api/health`, `/robots.txt`, `/api/debug/*`.

---

## 4. Вход «с нуля» — рамка ПОСЛЕ разговора, а не до

Три фазы (`src/lib/freeTalk.ts` + talk-действия в
`src/app/api/onboarding/route.ts`):

1. **Свободный разговор — его ведёт МОДЕЛЬ.** В промпте ни слова про
   слоты, и расписания вопросов нет вовсе: ни карты блоков, ни очереди,
   ни счётчиков в тексте. Сначала фаза понимания (`readFirstLine`,
   `src/lib/interviewer.ts`): дело / статус / не дело / отказ /
   растерянность. Дело названо — одна строка кода про экран, дальше
   говорит модель.
   **Каждый ход разбирается ДО записи** (`talkTurn.ts`): класс реплики
   (`answer` / `objection` / `question` / `garbage` / `skip`) и факты,
   разложенные по блокам. Подтверждение «Записал: клиенты — оптовики»
   существует только за факт и содержит факт с ярлыком блока, а не
   цитату; остальные четыре класса в карточку не пишутся.
   Панель слева заполняется ФАКТАМИ; блок «заполняется сейчас» — тот, о
   котором агент реально спросил (`questionBlock` по самой реплике), а
   не следующий по плану.
   **Отраслевая глубина — механизм `question_plan`** (`industryPlan.ts`):
   как только дело названо, `model_compile` строит план под сферу — чем
   наполнять каждый блок ИМЕННО в этом деле (пекарню про закваску и муку,
   юриста про специализацию и инстанции, оптовика про партии и отгрузку).
   План уходит в разговор ЗНАНИЕМ, а не расписанием: очередь и момент
   выбирает модель. Универсальный вопрос («кто твои клиенты») — брак,
   `isGenericQuestion` требует переспросить отраслевыми словами.
2. **Невидимый разбор.** Транскрипт уходит в тот же [GEN], что читает
   сайты (`generateCardFromTranscript`, `cardFromFacts.ts`) → карточка
   двенадцати слотов, те же контракты и приёмка.
3. **Точечный добор.** Один-два вопроса про недостающее — тем же
   механизмом, что добор на экране правки.

Конец решает судья сбоку («хватит/мало») плюс кнопка «Собрать карточку
сейчас»; жёсткий потолок — 15 реплик владельца.

### Сторожа кодом — важнее промптов

Каждый поставлен после живого провала; в коде рядом написано, какого.

- **В карточку идёт только сказанное владельцем** (`titleGuard.ts` +
  `ownFacts`): и заголовок, и каждый факт сверяются с его репликами по
  стемам. Это первый из двух оставшихся предохранителей; второй —
  публикация только после подтверждения заголовка.
- **Заголовок не выдумывается**: все стемы заголовка из реплик владельца
  — публикуем молча; появилось новое слово — флаг `INVENTED_TITLE`,
  вопрос «назовём карточку так?», и до ответа карточка **не публикуется**.
  Имени у дела может не быть вовсе: владелец его не называл — поле
  пустое, за карточку говорит заголовок.
- **Возражение стирает факт**: «нет, не это», «не консультации», «я не …»
  — оспоренный блок чистится на панели немедленно и переспрашивается.
- **Возражение против ВОПРОСА** («и так понятно») — не отказ: агент
  отвечает аргументом, зачем это агенту карточки, и переспрашивает с
  вариантами. Извинения запрещены промптом и ловятся `hasApology` с
  одной регенерацией — это та же капитуляция, что «сменим тему».
- **Должность ≠ дело**: «я руководитель региона» — честный ответ кодом,
  что карточка собирается под дело; класс `status` в классификаторе +
  `looksLikeStatus` на случай промаха модели.
- **Без лести**: открывашки («Круто!») и оценочные реакции на статус
  («Это большая ответственность») срезаются (`stripFlattery`).
- **Контакт — это номер, а не слово «телефон»** (`contactValue.ts`):
  контактом считается номер, почта, ник или ссылка; названный канал без
  адреса получает переспрос с примером. **Без валидного контакта карточка не
  публикуется** — правило держится во всех точках выхода, а как только
  контакт появился, карточка встаёт немедленно.
- **Агент карточки не повторяется**: два ответа подряд без нового факта →
  переход к контакту («Этого в карточке нет. Оставьте телефон — владелец
  ответит точно»), флаг `NO_FACT_HANDOFF` в трейсе.
- Валидатор реплик интервьюера (`interviewSpeech.ts`): предпосылки,
  рассогласование, двойной вопрос, канцелярит, повтор, реплика без
  вопроса — брак → одна регенерация → запасной вопрос.
- Применимость слотов к типу дела, неповтор закрытого, «короткое
  отрицание — не факт», готовность — всё кодом (`interviewer.ts`).
- В JS `\b` не видит кириллицу — границы слов в регулярках пишутся
  явно. На этой грабле проект стоял трижды.
- Модели-рассуждатели считают вслух внутри того же лимита: у deepseek-v4
  на сборку карточки ушло 4888 токенов из 5200, и наружу вышло «{».
  Механическим вызовам (строгий JSON) рассуждение выключено, запас лимита
  пропорционален задаче, обрыв пишется в лог с цифрами
  (`llm/openaiCompatible.ts`, `tests/reasoningBudget.test.ts`).

### Тестовые карточки

Карточка приёмочного прогона создаётся с `test: true` → адрес получает
префикс `gbtest-` (`testShop.ts`): noindex всегда, в robots закрыта целым
префиксом, любой будущий каталог/sitemap обязан фильтровать по
`isTestShop`, живёт 24 часа (первый заход после срока удаляет насовсем).
**Приёмочные прогоны не оставляют следов в проде.**

---

## 5. Чтение сайта (второй вход)

Трёхпроходный конвейер (`siteReader.ts` / `siteReadUnified.ts`): целевая
компания → атомарные утверждения с цитатами → отбор кодом → судья →
сборка с инспекциями. Краулер: 15 страниц, глубина 2, бюджет 40 с,
Jina Reader как обход бот-блока. Повторное чтение бесплатно —
кэш `site_reads` (миграция 015). **SSRF-защита `netGuard.ts` не
удаляется**; для e2e есть поимённая щель `GB_ALLOW_PRIVATE_HOSTS`
(точное имя хоста, в проде переменная не задана).

---

## 6. Модели и деньги

Роли разведены (`modelPlan.ts`, обоснования замерами лежат рядом):
`extract` — старшая модель, `compile` и `dialog` — дешёвая. Порядок:
переменная окружения → план → провайдер; всё видно в `/api/health`
вместе с `leadEmail`. Стоимость каждого вызова — в трейс. Владельцу
стоимость не показывается.

---

## 7. Карточка, витрина, машинный слой

- **Карточка v2** — двенадцать слотов (`card.ts`), контракты
  (`sanitizeCard`), приёмка (`cardAccept.ts`), применимость слотов к
  типу дела (`businessType.ts`: у эксперта нет каталога, у стартапа —
  цен). Источники полей подписываются честно: `owner` / `site` /
  `compiled`.
- **Постоянный идентификатор** (`shopId.ts`): `GB-` + шесть знаков без
  похожих `0/O` и `1/I` — ключ диктуют голосом и переписывают с экрана.
  Выдаётся при рождении карточки и не меняется никогда — ни при
  пересборке, ни при смене адреса. Slug — адрес для людей, идентификатор —
  ключ для машин; на нём держится посев («ваша карточка — GB-Q2GPQK»).
  Живёт **своей колонкой** `cards.identifier` под уникальным индексом
  (миграция 016) и дублируется внутри JSON; колонка старше. Коллизия
  громкая: индекс отвечает 23505, вставка берёт новый ключ и повторяет.
  Поиск по ключу — `getCardByIdentifier` (клейм, сверка реестра, саппорт).
  Виден в машинном слое витрины, в `identifier` JSON-ответа и в JSON-LD.
- **Правило индексации** (`cardIndexing.ts`): карточка без подтверждённого
  владельца не индексируется — мета страницы + `robots.txt` из базы.
- Витрина — вёрстка дизайнера (хендофф GB LONDON 10);
  `passportMarkup.ts` сгенерирован из `design/london10/**` и руками не
  правится. JSON-LD, `/api/lavka/<slug>.json` — из тех же данных.
- Экран правки `/t/<slug>/edit`: очередь работ, добор пробелов, блок
  «Заявки» сверху.

---

## 8. Агент карточки и заявки

Семиблочный промпт (ROLE/PROCEDURE/CONSTRAINTS/TONE + контекст), речь —
поставка куратора дословно; восемь правил речи продублированы кодом
(`speechCheck.ts`). Заявка посетителя уходит **письмом на почту из
слота 12** с флагами `LEAD_DELIVERED`/`LEAD_NOT_DELIVERED` в трейсе и
блоком «Заявки» на экране правки. **Resend и телеграм сняты с повестки —
не трогать.**

---

## 9. База

Supabase, только REST с сервис-ключом. Таблицы: `tenants`, `facts`,
`cards` (карточка v2 одним JSON, ключ правки, постоянный `identifier`
под уникальным индексом), `conversations`,
`messages`, `traces`, `leads`, `site_reads`. Миграции `sql/001…016`,
идемпотентные, запускаются владельцем в SQL Editor — **SQL владельцу
отдаётся текстом в чат**, файл в `sql/` создаётся всё равно.
Состояние разговора «с нуля» — `tenants.question_plan.interview`.

---

## 10. Проверки перед пушем — и правило моста

```bash
npm run typecheck
npm test          # юниты, включая дословность канона
npm run build
npm run e2e       # 4 пути целиком на моках сайта, базы и модели
./bridge.sh       # после мержа — обновить этот документ И опубликовать в gb-bridge
```

**Красный путь = не мержим.** Каждое изменение — тестом. Ход агента или
генерация без трейса — баг. Дефект, найденный и закрытый внутри захода,
наверх не выносится — он остаётся в коммитах и тестах.

---

## 11. Правила работы, которые лучше не нарушать

1. Промпты и тексты наружу поставляет куратор — дословно, свои
   формулировки не сочиняются (`prompts.ts`, `onboarding.ts`, `copy.ts`;
   дословность заперта тестами).
2. Пробел честнее выдумки; сторожа кодом, не увещеванием.
3. Ключи никогда не попадают в чат и в коммиты. Vercel руками не трогать.
4. Никаких новых механизмов, пока пути не зелёные.
5. Спека части 3 (MCP-адаптер) и 4 (лимиты) — не начинать.
6. Ссылки в переписке — только полные, с доменом.

---

## 12. Честный список недоделанного

| | состояние |
|---|---|
| Разговорный вход, три фазы, панель | **работает**, живые приёмки 05.08 |
| Чтение сайта, кэш чтения | **работает** |
| Карточка v2, витрина, правка, добор, заявки | **работает** |
| e2e: 4 пути на моках | **зелёные** (правило: красный = не мержим) |
| Домен | **нет** — блокер постоянных адресов |
| Мобильная вёрстка | не проверена на живом телефоне |
| Хендофф трёх состояний панели и мобильной полоски | ждём дизайнера; полоска пока собрана разработкой, вёрстка витрины не тронута |
| «Сознательно пустой» слот 2 как механизм схемы | не делали — только по слову куратора |
| Двойной вопрос в свободном разговоре | не трогаем — валидатор навредит больше (решение куратора 05.08) |

---

## 13. С чего начать первым днём

1. `npm ci && npm run typecheck && npm test && npm run build && npm run e2e`.
2. Открыть `/start`, написать «продаю мёд с пасеки», пройти разговор до
   ссылок, открыть карточку и экран правки. Пятнадцать минут — вся картина.
3. Прочитать `src/lib/freeTalk.ts` и talk-действия в
   `src/app/api/onboarding/route.ts` сверху донизу: комментарии
   объясняют не «что», а «почему», с живыми провалами, из-за которых
   код стал таким.
