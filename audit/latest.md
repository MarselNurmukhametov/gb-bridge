# Вердикт-лист по аудиту прода · 01.08

Аудит от 31.07, 35 пунктов. Работа шла по разделу «Порядок работ» (01–04);
блок 05 — вопросы владельцу, собраны внизу. Коммит с правками — «Аудит
прода: 26 пунктов одним заходом» в `gb-agent` (далее — «коммит аудита»).
Скрины всех экранов после правок — в `shots/`, кегли на каждом промерены
браузером.

## Сквозные

| № | Пункт | Вердикт |
|---|---|---|
| A1 | порог 40px ломает контролы | **исправлено · коммит аудита** — общий селектор снят; `.send-round` 40×40 (круг остался кругом), «повторить» и «Скопировать» держат форму и добирают зону паддингом, `.btn-skip` в списке, `.chip-test` получил `display:inline-flex` |
| A2 | серая лестница схлопнулась | **исправлено · коммит аудита** — `--text-mute` разведён (#66655F), «с сайта» #5F5E5A / «сказали вы» #6E6D68, лестница снова из четырёх ступеней |
| A3 | волоски светлее макета | **исправлено · коммит аудита** — `--hair` #E6E6E1, `--bd-card` #DCDCD7, `--bd-control` #CECEC9; C3 и C5 закрылись этим же |
| A4 | логотип 13.5 вместо 12.5 | **исправлено · коммит аудита** — 12.5px/500; проверка кеглей исключает логотип из порога 13px осознанно |
| A5 | scrollIntoView | **исправлено · предыдущий коммит** (фикс автоскролла): ref на саму ленту, `el.scrollTop = el.scrollHeight`, чат-экраны прижаты к вьюпорту — готово к виджету. Проверено браузером на длинном диалоге |

## /start · выбор тона

| № | Пункт | Вердикт |
|---|---|---|
| B1 | подстрока 16px | **исправлено · коммит аудита** — 14px, `--text-2` |
| B2 | карточка тона не с клавиатуры | **исправлено · коммит аудита** — `<button type="button">`, вид тот же, фокус `outline: 2px solid #3450C4; offset 2px` |
| B3 | блок незаконченной карточки не нарисован | **вопрос владельцу** — где показывать: над выбором тона (рекомендация дизайнера) или снизу. Код готов принять любой ответ |

## /start · разговор

| № | Пункт | Вердикт |
|---|---|---|
| C1 | лента не прижата к низу | **исправлено · коммит аудита** — колонке `min-height:100%` + `justify-content:flex-end`; короткий разговор стоит над полем |
| C2 | «Проверить агента» выпадает из ряда | **исправлено · коммит аудита** — `display:inline-flex; align-items:center` |
| C3 | граница дока и трек прогресса | **исправлено** через A3 (токены) |
| C4 | чипсы бледнее макета | **исправлено · коммит аудита** — бордер #CECEC9, текст #2C2C31 |
| C5 | волосок шапки, пузырь, fail-line | **исправлено** — `.hair-v` через `--bd-card` #DCDCD7, `--bd-user` #E2E2DD (регистр причёсан), `.fail-line` #6F5416 |

## Паспорт

| № | Пункт | Вердикт |
|---|---|---|
| D1 | нативный select «Добавить» | **исправлено · коммит аудита** — ghost-кнопка; группа с одним ключом добавляет сразу, с несколькими — раскрывает выбор текстовыми чипами (не нативный select). Если дизайнер нарисует свой выбор ключа — заменю на его вёрстку |
| D2 | карточка «Опубликовано» — четверть | **исправлено · коммит аудита** — четыре секции с волосками: статус · тест-драйв (текст аудита дословно) · «раздайте ссылку» (адрес mono 17px, «Скопировать», четыре канала дословно) · «что будет дальше» (три строки за левым волоском). Тексты «что будет дальше» написаны по описанию аудита — дизайнеру сверить формулировки |
| D3 | нет точки-маркера источника | **исправлено · коммит аудита** — точка 5px в цвет пометки, `display:flex; gap:5px` |
| D4 | значение факта 17px | **спорно** — владелец в задании от 31.07 явно попросил «значения фактов в паспорте 17px»; аудит предлагает 14.5 или пересчёт всей шкалы. Оставлено 17px до решения владельца с дизайнером |
| D5 | подзаголовок на всю колонку | **исправлено · коммит аудита** — `max-width:52ch`, `margin-top:9px` |
| D6 | лишний волосок под шапкой | **исправлено · коммит аудита** — граница снята; отступы страницы 20/22 уже стояли |
| D7 | карточка-пробел в столбик | **исправлено · коммит аудита** — строка `space-between`, тег uppercase + трекинг .08em, бордер #E6E6E1, паддинг 12/14 |
| D8 | зона публикации | **исправлено · коммит аудита** — кнопка и подпись в одну строку, кнопка 12/24 и 14px, отступы 40/20 |
| D9 | «Удалить» не нарисована | **вопрос владельцу** — удалять факт совсем или очищать в пробел (безопаснее, уже вписано в систему). До ответа кнопка остаётся как есть |

## Чат агента

| № | Пункт | Вердикт |
|---|---|---|
| E1 | чипы выбрасываются после приветствия | **исправлено · коммит аудита** — `chips: data.chips` в каждом сообщении агента |
| E2 | сбой сети стирает реплику | **исправлено · коммит аудита** — пузырь остаётся, под ним «Не отправилось · повторить»; повтор не плодит второй пузырь и не задваивает счётчик |
| E3 | карточка контакта без обещания | **исправлено · коммит аудита** — строка-обещание 13.5px из факта карточки `agent.cta` (не зашита; нет факта — строки нет, честно), primary на всю ширину, mono-подпись по центру |
| E4 | «контакт передан» | **исправлено · коммит аудита** — `.kicker`, линейка 1px, средняя часть текста вернулась (подставляется из `agent.cta`, фолбэк «он свяжется с вами») |
| E5 | плашка лимита тише | **исправлено · коммит аудита** — 14px основным цветом, радиус 10, паддинг 15/17 |
| E6 | золото «.ai» и мелочи шапки | **исправлено · коммит аудита** — .ai золотом в шапке чата, кикер предпросмотра #8A6A1F, letter-spacing заголовка, ссылка футера видима |
| E7 | виджет-размещение | **вопрос владельцу** — нужен срок. A5 (прокрутка) уже готов под виджет |

## /gb-debug

| № | Пункт | Вердикт |
|---|---|---|
| F1 | мобильной версии нет | **исправлено · коммит аудита** — `.tm-desktop`/`.tm-mobile` применены; строка ленты и лид на узком экране — карточка по раскладке аудита, разворот тот же, горизонтального скролла нет |
| F2 | «фактов: 6» вместо списка | **исправлено · коммит аудита** — mono-чипы с ключами фактов из `t.retrieved`, в строку с переносом |
| F3 | счётчики скачут | **исправлено · коммит аудита** — `font-variant-numeric: tabular-nums` |
| F4 | нет пульса у «обновляется каждые 5 с» | **исправлено · коммит аудита** — `.dot-live` мигает, на паузе серая и статичная |
| F5 | строки рывком, заголовки, формат | **исправлено · коммит аудита** — `gbfade` на вход строки; заголовки «тип ответа / вердикт / стоимость и время»; формат «0,4 ₽ · 1 240» с неразрывным пробелом |
| F6 | «сессия» вместо «бизнес» | **вопрос владельцу** — экран сейчас фактически на один бизнес; сказать, так ли задумано (тогда имя бизнеса поставлю в шапку экрана) или телеметрия нужна по всем |

## Вопросы владельцу (блок 05 аудита)

1. **B3** — блок «незаконченная карточка»: над выбором тона (рекомендация дизайнера) или снизу?
2. **D9** — удалять факт совсем или очищать в пробел (безопаснее)?
3. **E7** — срок по виджету 392×560: это половина ценности продукта, макет готов.
4. **F6** — телеметрия на один бизнес (имя в шапку) или на все?
5. **Лендинг** — главная в объёме или вычёркивается из пакета? Сейчас `/` ведёт на `/start`.
6. **D4** — значения фактов: оставить 17px (ваше задание) или 14.5px с пересчётом шкалы (предложение дизайнера)?

## Проверки после правок

- 146 юнит-тестов зелёные; мок-прогон e2e зелёный целиком.
- Скрины пяти экранов после правок — `shots/01…05`, кегли промерены:
  текста мельче 13px нет (единственное исключение — логотип 12.5px, по A4).
- Автоскролл перепроверен браузером после C1: прижатие, отлистывание,
  возврат — всё зелёное.

Деплой на Vercel уходит этим же пушем — ссылка для сверки дизайнером:
https://gb-agent-git-main-gb16.vercel.app/start
